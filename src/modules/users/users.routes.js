const express = require('express');

const router = express.Router();
// const { UsersController } = require('./users.controller');

// router.get('/users/my-profiles', UsersController.viewMyProfile);

module.exports = { userRouters: router };
